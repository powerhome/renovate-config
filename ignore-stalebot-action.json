{
  "packageRules": [
    {
      "description": "The stalebot workflow is rendered into every repo by terraform from powerhome/software. Scope this to the rendered path rather than to the actions/stale package, so the template it is rendered from stays updatable and a repo's own stale workflows are left alone.",
      "matchFileNames": [".github/workflows/stale.yml"],
      "enabled": false
    }
  ]
}
